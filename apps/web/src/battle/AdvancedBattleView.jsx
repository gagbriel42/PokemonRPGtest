import React,{useEffect,useMemo,useState}from'react';
import'./advanced-battle.css';
import{getHgssSpriteCandidates,spriteLabel}from'./hgssAssets.js';
const API='https://pokeapi.co/api/v2',DEX=1025;
const TYPES={normal:'NORMAL',fire:'FEU',water:'EAU',electric:'ÉLECTRIK',grass:'PLANTE',ice:'GLACE',fighting:'COMBAT',poison:'POISON',ground:'SOL',flying:'VOL',psychic:'PSY',bug:'INSECTE',rock:'ROCHE',ghost:'SPECTRE',dragon:'DRAGON',dark:'TÉNÈBRES',steel:'ACIER',fairy:'FÉE'};
const EN_TYPES={normal:'NORMAL',fire:'FIRE',water:'WATER',electric:'ELECTRIC',grass:'GRASS',ice:'ICE',fighting:'FIGHTING',poison:'POISON',ground:'GROUND',flying:'FLYING',psychic:'PSYCHIC',bug:'BUG',rock:'ROCK',ghost:'GHOST',dragon:'DRAGON',dark:'DARK',steel:'STEEL',fairy:'FAIRY'};
const FALLBACK_ITEMS=['Aucun','Potion','Super Potion','Hyper Potion','Potion Max','Guérison','Antidote','Anti-Para','Anti-Brûle','Anti-Gel','Réveil','Total Soin','Rappel','Rappel Max','Élixir','Max Élixir','Éther','Max Éther','Baie Sitrus','Baie Prine','Restes','Orbe Vie','Bandeau Choix','Lunettes Choix','Mouchoir Choix','Ceinture Force'];
const chart={normal:{rock:.5,ghost:0,steel:.5},fire:{fire:.5,water:.5,grass:2,ice:2,bug:2,rock:.5,dragon:.5,steel:2},water:{fire:2,water:.5,grass:.5,ground:2,rock:2,dragon:.5},electric:{water:2,electric:.5,grass:.5,ground:0,flying:2,dragon:.5},grass:{fire:.5,water:2,grass:.5,poison:.5,ground:2,flying:.5,bug:.5,rock:2,dragon:.5,steel:.5},ice:{fire:.5,water:.5,grass:2,ice:.5,ground:2,flying:2,dragon:2,steel:.5},fighting:{normal:2,ice:2,poison:.5,flying:.5,psychic:.5,bug:.5,rock:2,ghost:0,dark:2,steel:2,fairy:.5},poison:{grass:2,poison:.5,ground:.5,rock:.5,ghost:.5,steel:0,fairy:2},ground:{fire:2,electric:2,grass:.5,poison:2,flying:0,bug:.5,rock:2,steel:2},flying:{electric:.5,grass:2,fighting:2,bug:2,rock:.5,steel:.5},psychic:{fighting:2,poison:2,psychic:.5,dark:0,steel:.5},bug:{fire:.5,grass:2,fighting:.5,poison:.5,flying:.5,psychic:2,ghost:.5,dark:2,steel:.5,fairy:.5},rock:{fire:2,ice:2,fighting:.5,ground:.5,flying:2,bug:2,steel:.5},ghost:{normal:0,psychic:2,ghost:2,dark:.5},dragon:{dragon:2,steel:.5,fairy:0},dark:{fighting:.5,psychic:2,ghost:2,dark:.5,fairy:.5},steel:{fire:.5,water:.5,electric:.5,ice:2,rock:2,steel:.5,fairy:2},fairy:{fire:.5,fighting:2,poison:.5,dragon:2,dark:2,steel:.5}};
const title=s=>String(s||'').split('-').map(x=>x?x[0].toUpperCase()+x.slice(1):x).join(' '),clamp=(n,a,b)=>Math.max(a,Math.min(b,n)),sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function get(u){const r=await fetch(u);if(!r.ok)throw Error(r.status);return r.json()}
const cache=new Map();
async function frSpecies(id,fallback){const k=`s${id}`;if(cache.has(k))return cache.get(k);try{const d=await get(`${API}/pokemon-species/${id}`),n=d.names?.find(x=>x.language.name==='fr')?.name||fallback;cache.set(k,n);return n}catch{return fallback}}
async function frMove(id,fallback){const k=`m${id}`;if(cache.has(k))return cache.get(k);try{const d=await get(`${API}/move/${id}`),n=d.names?.find(x=>x.language.name==='fr')?.name||fallback;cache.set(k,n);return n}catch{return fallback}}
const expForLevel=l=>100*l*l,levelFromExp=x=>clamp(Math.floor(Math.sqrt(Math.max(0,x)/100)),1,100),nextExp=l=>100*(l+1)*(l+1),statusName=s=>({paralysis:'Paralysie',poison:'Poison',burn:'Brûlure',freeze:'Gel',sleep:'Sommeil',badlyPoisoned:'Poison grave'}[s]||s);
async function hydrate(ref,level=50){const d=await get(ref.url||`${API}/pokemon/${ref.id}`),stats={};for(const s of d.stats||[])stats[s.stat.name.replace('special-attack','specialAttack').replace('special-defense','specialDefense')]=s.base_stat;const name=await frSpecies(d.id,title(d.name));const eligible=(d.moves||[]).flatMap(m=>(m.version_group_details||[]).filter(v=>v.move_learn_method.name==='level-up'&&v.level_learned_at<=level).map(v=>({url:m.move.url,level:v.level_learned_at}))).sort((a,b)=>a.level-b.level);const uniq=[];for(const m of eligible)if(!uniq.some(x=>x.url===m.url))uniq.push(m.url);let moves=(await Promise.all(uniq.slice(-12).map(async u=>{try{const x=await get(u);return{id:x.id,name:await frMove(x.id,title(x.name)),power:x.power||0,accuracy:x.accuracy||100,pp:x.pp||1,maxPP:x.pp||1,type:x.type?.name||'normal',damageClass:x.damage_class?.name||'status',priority:x.priority||0,critRate:x.meta?.crit_rate||0,ailment:x.meta?.ailment?.name||null,ailmentChance:x.meta?.ailment_chance||0,effect:x.effect_entries?.find(e=>e.language.name==='fr')?.short_effect||x.effect_entries?.find(e=>e.language.name==='en')?.short_effect||'Aucun effet.'}}catch{return null}}))).filter(Boolean);moves=moves.filter((m,i,a)=>a.findIndex(x=>x.id===m.id)===i).slice(-4);if(!moves.length)moves=[{id:33,name:'Charge',power:40,accuracy:100,pp:35,maxPP:35,type:'normal',damageClass:'physical',priority:0,critRate:0,ailment:null,ailmentChance:0,effect:'Inflige des dégâts.'}];const lv=level||50,hp=Math.max(1,Math.floor((2*(stats.hp||50)*lv)/100)+lv+10);return{...ref,id:d.id,name,level:lv,exp:expForLevel(lv),hp,maxHp:hp,stats,types:(d.types||[]).sort((a,b)=>a.slot-b.slot).map(x=>x.type.name),moves,heldItem:ref.heldItem||'Aucun',status:null,statusTurns:0,mega:false,dynamax:false,tera:false,teraType:d.types?.[0]?.type?.name||'normal',capturable:!!ref.capturable}}
function effectiveness(m,d){return(d.types||[]).reduce((v,t)=>v*(chart[m.type]?.[t]??1),1)}
function damage(a,d,m){if(m.damageClass==='status'||!m.power)return{damage:0,effect:1,critical:false,miss:false};if(Math.random()*100>=m.accuracy)return{damage:0,effect:1,critical:false,miss:true};const A=a.stats?.[m.damageClass==='special'?'specialAttack':'attack']||50,D=d.stats?.[m.damageClass==='special'?'specialDefense':'defense']||50,eff=effectiveness(m,d),stab=a.types.includes(m.type)?1.5:1,crit=Math.random()<((1+m.critRate)/24),boost=a.dynamax?1.25:a.mega?1.15:a.tera?1.2:1,base=Math.floor((((2*a.level/5+2)*m.power*A/Math.max(1,D))/50)+2);return{damage:eff===0?0:Math.max(1,Math.floor(base*stab*boost*(crit?1.5:1)*eff*(.85+Math.random()*.15))),effect:eff,critical:crit,miss:false}}
function maxMove(m){return{...m,id:`max-${m.id}`,name:`Max ${TYPES[m.type]||'Normal'}`,power:Math.max(90,m.power?Math.min(150,Math.floor(m.power*1.5)):100),accuracy:100,pp:1,maxPP:1,damageClass:m.damageClass,effect:`Attaque Dynamax de type ${TYPES[m.type]||m.type}.`}}
function applyStatus(target,m){if(!m.ailment||!m.ailmentChance||target.status)return null;if(Math.random()*100>=m.ailmentChance)return null;const s=m.ailment==='paralysis'?'paralysis':m.ailment==='burn'?'burn':m.ailment==='freeze'?'freeze':m.ailment==='sleep'?'sleep':m.ailment==='poison'?'poison':m.ailment==='badly-poisoned'?'badlyPoisoned':null;return s?{...target,status:s,statusTurns:s==='sleep'?2:1}:null}
function Hp({p}){return <div className="ab-hp"><i style={{width:`${clamp(100*p.hp/Math.max(1,p.maxHp),0,100)}%`}}/></div>}
function Sprite({pokemon,side}){const[i,setI]=useState(0),src=getHgssSpriteCandidates(pokemon,side)[i];return <div className="ab-sprite">{src&&<img src={src} alt={spriteLabel(pokemon.name,side)} onError={()=>setI(x=>x+1)}/>}</div>}
function ItemPicker({items,value,onChange}){const[q,setQ]=useState('');const list=items.filter(x=>x.toLowerCase().includes(q.toLowerCase()));return <div className="item-picker"><input placeholder="Rechercher un objet…" value={q} onChange={e=>setQ(e.target.value)}/><select value={value} onChange={e=>onChange(e.target.value)}>{list.map(x=><option key={x}>{x}</option>)}</select></div>}
function Picker({team,setTeam}){const[list,setList]=useState([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{get(`${API}/pokemon?limit=${DEX}`).then(d=>setList(d.results.map((x,i)=>({id:i+1,name:title(x.name),url:x.url})))).finally(()=>setLoading(false))},[]);const filtered=useMemo(()=>{const s=q.toLowerCase().trim();return list.filter(p=>!s||String(p.id).includes(s)||p.name.toLowerCase().includes(s))},[list,q]);return <div className="ab-picker"><input autoFocus placeholder="Rechercher par nom ou numéro…" value={q} onChange={e=>setQ(e.target.value)}/>{loading?<p>Chargement…</p>:<div className="ab-picker-grid">{filtered.slice(0,120).map(p=><button key={p.id} disabled={team.some(x=>x.id===p.id)||team.length>=6} onClick={()=>setTeam([...team,{...p,level:50,heldItem:'Aucun',exp:expForLevel(50),capturable:false}])}><img src={`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/${p.id}.png`} alt=""/><span>#{p.id} {p.name}</span></button>)}</div>}</div>}
function ConfigureTeam({label,team,setTeam,items,gm,onXp}){const[open,setOpen]=useState(false),[search,setSearch]=useState('');const update=(i,k,v)=>setTeam(team.map((p,j)=>j===i?{...p,[k]:v}:p));return <section className="ab-card"><header><b>{label}</b><span>{team.length}/6</span></header>{team.map((p,i)=><div className="ab-config" key={`${p.id}-${i}`}><img src={`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/${p.id}.png`} alt=""/><div><strong>{p.name}</strong><label>Nv <input type="number" min="1" max="100" value={p.level||50} onChange={e=>update(i,'level',clamp(+e.target.value||1,1,100))}/></label><label>EXP <input type="number" min="0" value={p.exp??expForLevel(p.level||50)} onChange={e=>{const exp=Math.max(0,+e.target.value||0);update(i,'exp',exp);update(i,'level',levelFromExp(exp))}}/></label><label>Objet <ItemPicker items={items} value={p.heldItem||'Aucun'} onChange={v=>update(i,'heldItem',v)}/></label>{gm&&<label>+EXP <input type="number" placeholder="0" onChange={e=>onXp?.(i,+e.target.value||0)}/></label>}</div><button onClick={()=>setTeam(team.filter((_,j)=>j!==i))}>×</button></div>)}<input className="ab-filter" placeholder="Filtrer l'équipe…" value={search} onChange={e=>setSearch(e.target.value)}/><button className="ab-add" disabled={team.length>=6} onClick={()=>setOpen(true)}>+ AJOUTER UN POKÉMON</button>{open&&<div className="ab-modal"><div className="ab-modal-box"><button className="ab-close" onClick={()=>setOpen(false)}>×</button><Picker team={team} setTeam={setTeam}/></div></div>}</section>}
function SavePanel({profile,setProfile,team,setTeam,items,gm}){const[slot,setSlot]=useState('');const save=()=>{if(!profile.name.trim())return;const data={...profile,team};localStorage.setItem(`pokemon-jdr-save:${profile.name}`,JSON.stringify(data));setSlot(profile.name)};const load=()=>{if(!slot)return;const d=JSON.parse(localStorage.getItem(`pokemon-jdr-save:${slot}`)||'null');if(d){setProfile(d);setTeam(d.team||[])}};return <section className="ab-card save-panel"><header><b>SAUVEGARDE DU JOUEUR</b><span>locale</span></header><label>Nom <input value={profile.name} onChange={e=>setProfile({...profile,name:e.target.value})}/></label><label>Argent ₽ <input type="number" min="0" value={profile.money} onChange={e=>setProfile({...profile,money:Math.max(0,+e.target.value||0)})}/></label><label>Objets du sac <input value={profile.bag.join(', ')} onChange={e=>setProfile({...profile,bag:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)})}/></label>{gm&&<p className="gm-note">Le MJ peut modifier le nom, l'argent, le sac, l'équipe et l'EXP avant de sauvegarder.</p>}<div className="save-actions"><button onClick={save}>SAUVEGARDER</button><button onClick={load}>CHARGER</button></div></section>}
function Setup({onStart,onExit,gm=false,lang='fr'}){const[player,setPlayer]=useState([]),[enemy,setEnemy]=useState([]),[items,setItems]=useState(FALLBACK_ITEMS),[gimmick,setGimmick]=useState('none'),[wild,setWild]=useState(false),[profile,setProfile]=useState({name:'',money:3000,bag:['Potion','Poké Ball']});useEffect(()=>{get(`${API}/item?limit=500`).then(async d=>{const names=await Promise.all(d.results.map(async x=>{try{const z=await get(x.url);return z.names?.find(n=>n.language.name==='fr')?.name||title(x.name)}catch{return title(x.name)}}));setItems(['Aucun',...names.filter(Boolean).filter((x,i,a)=>a.indexOf(x)===i)])}).catch(()=>{})},[]);const start=async()=>{if(!player.length||!enemy.length)return;const[p,e]=await Promise.all([Promise.all(player.map(x=>hydrate(x,x.level))),Promise.all(enemy.map(x=>hydrate({...x,capturable:wild&&x.capturable},x.level)))]);onStart(p,e,gimmick,items,{...profile,team:p},wild)};return <div className="ab-shell"><header className="ab-header"><button onClick={onExit}>← {lang==='fr'?'QUITTER':'EXIT'}</button><div><b>{gm?'MJ · ':'Joueur · '}PRÉPARATION DU COMBAT</b><small>Objets individuels · progression persistante · 3× EXP officielle</small></div><div className="ab-lang"><button>FR</button><button>EN</button></div></header><main className="ab-setup-grid"><ConfigureTeam label="ÉQUIPE ALLIÉE" team={player} setTeam={setPlayer} items={items} gm={gm}/><ConfigureTeam label="ÉQUIPE ENNEMIE" team={enemy} setTeam={setEnemy} items={items} gm={gm}/><section className="ab-card"><header><b>RÈGLES</b></header><p>Les objets peuvent être attribués avant le combat. Une équipe sauvage peut être capturée si chaque Pokémon autorisé est coché.</p><label className="ab-check"><input type="checkbox" checked={wild} onChange={e=>{setWild(e.target.checked);setEnemy(enemy.map(p=>({...p,capturable:e.target.checked?p.capturable:false})))}}/> Combat sauvage / capture activable</label>{enemy.map((p,i)=><label className="ab-check" key={i}><input type="checkbox" checked={!!p.capturable} onChange={e=>setEnemy(enemy.map((x,j)=>j===i?{...x,capturable:e.target.checked}:x))}/>{p.name} capturable</label>)}<div className="ab-gimmicks">{[['none','NORMAL'],['mega','MÉGA'],['dynamax','DYNAMAX'],['tera','TÉRA']].map(([v,n])=><button className={gimmick===v?'sel':''} key={v} onClick={()=>setGimmick(v)}>{n}</button>)}</div></section><SavePanel profile={profile} setProfile={setProfile} team={player} setTeam={setPlayer} items={items} gm={gm}/></main><footer className="ab-footer"><span>Allié {player.length}/6</span><span>Ennemi {enemy.length}/6</span><button disabled={!player.length||!enemy.length} onClick={start}>⚔ LANCER LE COMBAT</button></footer></div>}
function Battle({playerTeam,enemyTeam,onExit,gimmick,items,profile,wild,onSave}){const[player,setPlayer]=useState(playerTeam),[enemy,setEnemy]=useState(enemyTeam),[pi,setPi]=useState(0),[ei,setEi]=useState(0),[log,setLog]=useState(['Le combat commence !']),[busy,setBusy]=useState(false),[info,setInfo]=useState(null),[bag,setBag]=useState(false),[used,setUsed]=useState(false),[capture,setCapture]=useState(false),[reward,setReward]=useState(null);const me=player[pi],foe=enemy[ei],say=m=>setLog(x=>[...x.slice(-16),m]);const update=(arr,i,p)=>arr.map((x,j)=>j===i?p:x);const end=won=>{if(!won)return;const base=enemy.reduce((s,p)=>s+p.level*18,0),money=Math.round(base*(enemy.length*.9+player.filter(p=>p.hp>0).length*.25));const total=base*3;const xpEach=Math.round(total/Math.max(1,player.filter(p=>p.hp>0).length));setPlayer(player.map(p=>p.hp>0?({...p,exp:(p.exp||expForLevel(p.level))+xpEach,level:levelFromExp((p.exp||expForLevel(p.level))+xpEach)}):p));setReward({xp:xpEach,money,won});onSave?.(money,xpEach)};const doMove=async raw=>{if(busy||!me||!foe||me.hp<=0)return;setBusy(true);let m=raw;if(me.dynamax)m=maxMove(m);const usedMove={...m,pp:Math.max(0,m.pp-1)},attacker={...me,moves:me.moves.map(x=>x.id===raw.id?{...x,pp:Math.max(0,x.pp-1)}:x)},r=damage(attacker,foe,usedMove);let pArr=player,eArr=enemy;if(r.miss)say(`${me.name} utilise ${raw.name}, mais l'attaque échoue !`);else{let target={...foe,hp:Math.max(0,foe.hp-r.damage)},st=applyStatus(target,raw);if(st)target=st;eArr=update(enemy,ei,target);say(`${me.name} utilise ${m.name}${r.damage?` et inflige ${r.damage} dégâts.`:'.'}`);if(r.critical)say('Coup critique !');if(r.effect>1)say('C’est super efficace !');if(r.effect>0&&r.effect<1)say('Ce n’est pas très efficace…');if(r.effect===0)say('Cela n’a aucun effet !');if(st)say(`${target.name} est maintenant ${statusName(st.status)} !`);setInfo(raw)}pArr=update(player,pi,attacker);setPlayer(pArr);setEnemy(eArr);await sleep(500);let active=eArr[ei];if(active?.hp<=0){say(`${active.name} est K.O. !`);const n=eArr.findIndex((p,i)=>i!==ei&&p.hp>0);if(n>=0){setEi(n);active=eArr[n];say(`L'ennemi envoie ${active.name} !`)}else{say('Victoire !');end(true);setBusy(false);return}}const target=pArr[pi];if(active?.hp>0&&target?.hp>0){if(target.status==='paralysis'&&Math.random()<.25){say(`${target.name} est paralysé et ne peut pas agir !`)}else{const choices=active.moves.filter(x=>x.pp>0),ai=choices[Math.floor(Math.random()*choices.length)]||active.moves[0],ar=damage(active,target,ai);if(ar.miss)say(`${active.name} rate ${ai.name}.`);else{let nd={...target,hp:Math.max(0,target.hp-ar.damage)},st=applyStatus(nd,ai);if(st)nd=st;pArr=update(pArr,pi,{...nd,moves:target.moves});say(`${active.name} utilise ${ai.name}${ar.damage?` et inflige ${ar.damage} dégâts.`:'.'}`);if(st)say(`${nd.name} est ${statusName(nd.status)} !`);if(nd.hp<=0){say(`${nd.name} est K.O. !`);const n=pArr.findIndex((p,i)=>i!==pi&&p.hp>0);if(n>=0){setPi(n);say(`À toi, ${pArr[n].name} !`)}}setPlayer(pArr)}}}setBusy(false)};const useGimmick=()=>{if(used||!me||gimmick==='none')return;const key=gimmick==='mega'?'mega':gimmick==='dynamax'?'dynamax':'tera';setPlayer(update(player,pi,{...me,[key]:true}));setUsed(true);say(`${me.name} active ${gimmick==='mega'?'la Méga-Évolution':gimmick==='dynamax'?'le Dynamax':'la Téracristallisation'} !`)};const useItem=name=>{let p={...me};if(name==='Potion')p.hp=Math.min(p.maxHp,p.hp+20);else if(name==='Super Potion')p.hp=Math.min(p.maxHp,p.hp+50);else if(name==='Hyper Potion')p.hp=Math.min(p.maxHp,p.hp+120);else if(name==='Potion Max'||name==='Guérison')p.hp=p.maxHp;else if(['Élixir','Max Élixir','Éther','Max Éther'].includes(name))p.moves=p.moves.map(m=>({...m,pp:m.maxPP}));else if(name==='Antidote'||name==='Anti-Para'||name==='Anti-Brûle'||name==='Anti-Gel'||name==='Réveil'||name==='Total Soin'){p.status=null;p.statusTurns=0;say(`${p.name} est soigné.`)}else{say(`${name} ne produit pas encore d'effet automatisé.`);setBag(false);return}setPlayer(update(player,pi,p));setBag(false);say(`Vous utilisez ${name} sur ${p.name}.`)};const tryCapture=()=>{if(!wild||!foe?.capturable||foe.hp>Math.max(1,foe.maxHp*.35)){say('Ce Pokémon ne peut pas être capturé dans ces conditions.');return}if(Math.random()<.55){say(`${foe.name} est capturé !`);setCapture(true)}else say(`${foe.name} s'échappe de la Poké Ball !`)};return <div className="ab-shell battle"><header className="ab-header"><button onClick={onExit}>← QUITTER</button><div><b>COMBAT POKÉMON</b><small>{player.filter(p=>p.hp>0).length}/6 alliés · {enemy.filter(p=>p.hp>0).length}/6 ennemis</small></div><span>{profile?.name||'Joueur'} · ₽ {profile?.money||0}</span></header><div className="ab-stage"><div className="ab-fighter enemy"><div className="ab-status"><b>{foe?.name||'—'}</b><span>Nv. {foe?.level}</span><Hp p={foe||{hp:0,maxHp:1}}/><small>{foe?.types?.map(t=>TYPES[t]).join(' / ')} · {foe?.status?statusName(foe.status):'OK'}</small></div>{foe&&<Sprite pokemon={foe} side="front"/>}</div><div className="ab-fighter player"><div className="ab-status"><b>{me?.name||'—'}</b><span>Nv. {me?.level}</span><Hp p={me||{hp:0,maxHp:1}}/><small>{me?.types?.map(t=>TYPES[t]).join(' / ')} · {me?.status?statusName(me.status):'OK'}</small></div>{me&&<Sprite pokemon={me} side="back"/>}</div></div><div className="ab-battle-grid"><section className="ab-log">{log.map((x,i)=><div key={i}>{x}</div>)}</section><section className="ab-moves">{me?.moves.map(m=><button key={m.id} disabled={busy||m.pp<=0||!foe||foe.hp<=0} onMouseEnter={()=>setInfo(m)} onClick={()=>doMove(m)}><b>{me.dynamax?maxMove(m).name:m.name}</b><span>{TYPES[m.type]} · PP {m.pp}/{m.maxPP}</span></button>)}</section></div><div className="ab-actions"><button onClick={()=>setBag(true)} disabled={busy}>SAC</button><button disabled={busy} onClick={()=>{const n=player.findIndex((p,i)=>i!==pi&&p.hp>0);if(n>=0){setPi(n);say(`À toi, ${player[n].name} !`)}}}>POKÉMON</button><button disabled={busy||gimmick==='none'||used} onClick={useGimmick}>{gimmick==='dynamax'?'DYNAMAX':gimmick==='mega'?'MÉGA':'TÉRA'}</button>{wild&&<button disabled={busy||capture} onClick={tryCapture}>CAPTURE</button>}</div><div className="ab-tooltip">{info?<><b>{info.name}</b><span>{info.effect}</span></>:<>Sélectionnez une attaque pour voir sa description.</>}</div>{bag&&<div className="ab-modal"><div className="ab-modal-box bag"><button className="ab-close" onClick={()=>setBag(false)}>×</button><h2>SAC</h2><input placeholder="Rechercher un objet…"/><div>{items.map(x=><button key={x} onClick={()=>useItem(x)}>{x}</button>)}</div></div></div>}{reward&&<div className="ab-modal"><div className="ab-modal-box result"><h2>FIN DU COMBAT</h2><p>Victoire. Récompense : <b>+{reward.xp} EXP</b> par Pokémon ayant combattu et <b>+₽ {reward.money}</b>.</p><p>EXP calculée à <b>3×</b> la base officielle.</p>{capture&&<p>Le Pokémon capturé a été ajouté à l'équipe sauvegardée.</p>}<button onClick={onExit}>CONTINUER</button></div></div>}</div>}
export default function AdvancedBattleView({onExit,gm=false,lang='fr'}){const[start,setStart]=useState(null);const begin=(p,e,g,i,profile,wild)=>setStart({p,e,g,items:i,profile,wild});if(start)return <Battle {...start} onExit={()=>setStart(null)} onSave={(money,xp)=>{const p={...start.profile,money:(start.profile?.money||0)+money};if(p.name)localStorage.setItem(`pokemon-jdr-save:${p.name}`,JSON.stringify({...p,team:start.p}));setStart(s=>({...s,profile:p}))}/>;return <Setup onExit={onExit} onStart={begin} gm={gm} lang={lang}/>}
export function GmAdvancedBattleView({onExit,lang='fr'}){return <AdvancedBattleView onExit={onExit} gm lang={lang}/>}
